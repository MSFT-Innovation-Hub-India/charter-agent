pywebview>=5.0
pyyaml>=6.0
httpx>=0.27
azure-identity>=1.17
# Foundry hosted-agent client SDK (preview). Provides the idiomatic transport
# used in hosted mode: AIProjectClient.beta.agents.create_session() to mint a
# server-side session and get_openai_client().responses.create() to drive the
# Responses API with typed streaming events. The raw-httpx path is retained as
# the local-mode transport and as an automatic fallback if this is unavailable.
azure-ai-projects>=2.1.0
# Optional but strongly preferred on Windows: enables the native WAM account
# picker (modal to the app window) instead of opening the system browser.
# Falls back to InteractiveBrowserCredential automatically if missing.
azure-identity-broker>=1.3
# Optional: enables Windows toast notifications for the autonomous background poller.
# Gracefully absent — the poller runs silently without it.
winotify>=1.1
